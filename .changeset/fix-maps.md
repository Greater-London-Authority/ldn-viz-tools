---
'@ldn-viz/maps': patch
---

FIXED: upgrade `terra-draw` and `terra-draw-maplibre-gl-adapter` to fix bug in `MapDraw` component (features would immediately disappear after user had finished editing them)
