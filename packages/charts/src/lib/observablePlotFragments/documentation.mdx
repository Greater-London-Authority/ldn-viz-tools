import { Meta } from '@storybook/blocks';

import * as snippets from './observablePlotFragments.ts';
import { getDefaultPlotStyles } from './observablePlotFragments.ts';

<Meta title="Charts/ObservablePlot/Reusable Fragments" />

# Observable Plot fragments

The library defines a number of reusable fragments of Observable Plot configuration, to allow plots to be consistently styled with less copy-and-paste of code.

### General styles

`fontStack` is:

<pre>{snippets.fontStack}</pre>

In light mode, `defaultStyle` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultStyle, null, 2)}</pre>

In dark mode, `defaultStyle` is:

<pre>{JSON.stringify(getDefaultPlotStyles('dark').defaultStyle, null, 2)}</pre>

`defaultSize` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultSize, null, 2)}</pre>

`defaultSizeFacet` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultSizeFacet, null, 2)}</pre>

`defaultColor` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultColor, null, 2)}</pre>

### Axis styles

`defaultXAxis` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultXAxis, null, 2)}</pre>

`defaultYAxis` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultYAxis, null, 2)}</pre>

### Line styles

In light mode, `defaultLine` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultLine, null, 2)}</pre>

In dark mode, `defaultLine` is:

<pre>{JSON.stringify(getDefaultPlotStyles('dark').defaultLine, null, 2)}</pre>

`defaultDashedLine` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultDashedLine, null, 2)}</pre>

### Scales

`defaultXScale` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultXScale, null, 2)}</pre>

`defaultYScale` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultYScale, null, 2)}</pre>

### Grid

In light mode, `defaultGridX` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultGridX, null, 2)}</pre>

In dark mode, `defaultGridX` is:

<pre>{JSON.stringify(getDefaultPlotStyles('dark').defaultGridX, null, 2)}</pre>

### Styles for other marks

In light mode, `defaultDot` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultDot, null, 2)}</pre>

In dark mode, `defaultDot` is:

<pre>{JSON.stringify(getDefaultPlotStyles('dark').defaultDot, null, 2)}</pre>

In light mode, `defaultPoint` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultPoint, null, 2)}</pre>

In dark mode, `defaultPoint` is:

<pre>{JSON.stringify(getDefaultPlotStyles('dark').defaultPoint, null, 2)}</pre>

**Note:** in order to use `defaultPoint` styles, you should use our API, `Plot.point` which customises Observable's `Plot.dot` under the hood.

In light mode, `defaultArea` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultArea, null, 2)}</pre>

In dark mode, `defaultArea` is:

<pre>{JSON.stringify(getDefaultPlotStyles('dark').defaultArea, null, 2)}</pre>

In light mode, `defaultRule` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultRule, null, 2)}</pre>

In dark mode, `defaultRule` is:

<pre>{JSON.stringify(getDefaultPlotStyles('dark').defaultRule, null, 2)}</pre>

### Annotations

In light mode, `defaultAnnotationText` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultAnnotationText, null, 2)}</pre>

In dark mode, `defaultAnnotationText` is:

<pre>{JSON.stringify(getDefaultPlotStyles('dark').defaultAnnotationText, null, 2)}</pre>

In light mode, `defaultAnnotationTip` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultAnnotationTip, null, 2)}</pre>

In dark mode, `defaultAnnotationTip` is:

<pre>{JSON.stringify(getDefaultPlotStyles('dark').defaultAnnotationTip, null, 2)}</pre>

In light mode, `defaultAnnotationRange` is:

<pre>{JSON.stringify(getDefaultPlotStyles('light').defaultAnnotationTip, null, 2)}</pre>

In dark mode, `defaultAnnotationRange` is:

<pre>{JSON.stringify(getDefaultPlotStyles('dark').defaultAnnotationTip, null, 2)}</pre>
