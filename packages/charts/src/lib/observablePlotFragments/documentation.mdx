import { Meta } from '@storybook/addon-docs/blocks';

import * as snippets from './observablePlotFragments.ts';
import { getDefaultPlotStyles } from './observablePlotFragments.ts';

<Meta title="Charts/Components/ObservablePlot/Reusable Fragments" />

# Observable Plot fragments

The library defines a number of reusable fragments of Observable Plot configuration, to allow plots to be consistently styled with less copy-and-paste of code.

### General styles

`fontStack` is:

<pre>{snippets.fontStack}</pre>

In the curent mode, `defaultStyle` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultStyle, null, 2)}</pre>

`defaultSize` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultSize, null, 2)}</pre>

`defaultSizeFacet` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultSizeFacet, null, 2)}</pre>

`defaultColor` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultColor, null, 2)}</pre>

### Axis styles

`defaultXAxis` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultXAxis, null, 2)}</pre>

`defaultYAxis` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultYAxis, null, 2)}</pre>

### Line styles

In the curent mode, `defaultLine` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultLine, null, 2)}</pre>

`defaultDashedLine` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultDashedLine, null, 2)}</pre>

### Scales

`defaultXScale` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultXScale, null, 2)}</pre>

`defaultYScale` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultYScale, null, 2)}</pre>

### Grid

In the curent mode, `defaultGridX` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultGridX, null, 2)}</pre>

### Styles for other marks

In the curent mode, `defaultDot` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultDot, null, 2)}</pre>

In the curent mode, `defaultPoint` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultPoint, null, 2)}</pre>

**Note:** in order to use `defaultPoint` styles, you should use our API, `Plot.point` which customises Observable's `Plot.dot` under the hood.

In the curent mode, `defaultArea` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultArea, null, 2)}</pre>

In the curent mode, `defaultRule` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultRule, null, 2)}</pre>

### Annotations

In the curent mode, `defaultAnnotationText` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultAnnotationText, null, 2)}</pre>

In the curent mode, `defaultAnnotationTip` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultAnnotationTip, null, 2)}</pre>

In the curent mode, `defaultAnnotationRange` is:

<pre>{JSON.stringify(getDefaultPlotStyles().defaultAnnotationRange, null, 2)}</pre>
