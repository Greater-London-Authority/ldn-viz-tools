import { Meta } from '@storybook/blocks';
import '../app.postcss';

<Meta title="Tables/Introduction" />

# Welcome to Storybook for ldn-viz-tools tables

This provides a component for rendering tables.
It includes support for rendering visulaizations inside table cells, and for rendering summary visualizations for columns.
