import { Controls, Meta, Subtitle, Title } from '@storybook/blocks';
import * as InputStories from './Input.stories.svelte';

<Meta title="Ui/Input" of={InputStories} />

<Title />
<Subtitle />
The input component consists of a wrapper component that adds a 'chrome' to an input. This handles
things like labels, descriptions, tooltips etc. though it's own set of properties, listed below. You
only need to import and use the input component in your code as the wrapper component inside
implemented in the input component file.

<Controls />
