import { Meta } from '@storybook/blocks';

<Meta title="Ui/Introduction" />

# Welcome to Storybook for ldn-viz-tools ui elements

## A collection of components and patterns for rapid, consistent data visualization design for the City Intelligence Unit at the Greater London Authority.

Storybook helps you build UI components in isolation from your app's business logic, data, and context.
That makes it easy to develop hard-to-reach states. Save these UI states as **stories** to revisit during development, testing, or QA.

### Form element styling

Styling native form elements to be consistent cross browser can be a pain.
We provide a basic reset for form styles [via The Tailwind form plugin](https://github.com/tailwindlabs/tailwindcss-forms)
We also provide a set of themed form classes for use in this library and in your projects.

To be flexible when inegrating with existing projects We don't gererate global form styles, however the theme styles can be applied via class names:

#### tldr; Apply the `.form-checkbox` class to a native checkbox and it will be themed the same as an import of `<Checkbox />` (though it will only have native functionality).

Here is a complete table of the provided `form-*` classes for reference:

| Base                      | Class              |
| ------------------------- | ------------------ |
| `[type='text']`           | `form-input`       |
| `[type='email']`          | `form-input`       |
| `[type='url']`            | `form-input`       |
| `[type='password']`       | `form-input`       |
| `[type='number']`         | `form-input`       |
| `[type='date']`           | `form-input`       |
| `[type='datetime-local']` | `form-input`       |
| `[type='month']`          | `form-input`       |
| `[type='search']`         | `form-input`       |
| `[type='tel']`            | `form-input`       |
| `[type='time']`           | `form-input`       |
| `[type='week']`           | `form-input`       |
| `textarea`                | `form-textarea`    |
| `select`                  | `form-select`      |
| `select[multiple]`        | `form-multiselect` |
| `[type='checkbox']`       | `form-checkbox`    |
| `[type='radio']`          | `form-radio`       |
|                           | `form-label`       |

```html
<!-- apply our theme to any native form element with a single class -->
<input type="text" class="form-input" />

<!-- A full example using checkbox-->
<label class="flex items-center">
	<input class="form-checkbox" type="checkbox" bind:checked />
	<span class="mx-2 form-label">Checkbox Option</span>
</label>

<!-- further customise using utility classes -->
<input type="text" class="form-input text-core-red-500 w-full" />
```
